odoo.define("pos_discount_auth.config", function (require) {
  "use strict";

  // const module = require("point_of_sale.models");
  // const PosModelExtend = module.PosModel;
  // const models = PosModelExtend.prototype.models;
  // for (const i = 0; i < models.length; i++) {
  //   const model = models[i];
  //   if (model.model === "pos.config") {
  //     model.fields.push("authorized_code");
  //   }
  // }
});
